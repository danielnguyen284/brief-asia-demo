# BriefAsia Design Folder

The old `design/project/` Claude Design handoff bundle has been removed because
it was copied forward from DTW and conflicted with the current BriefAsia visual
system.

Current source of truth:

1. `../DESIGN.md` for design rules, tokens, typography, and implementation
   constraints.
2. `../public/design-prototype.html` for the visual prototype that `DESIGN.md`
   was normalized against.
3. `../BriefAsia (full site).html` for content and page-parity reference when
   needed.
